services:
  fractol:
    build:
      context: .
      dockerfile: docker/Dockerfile
    environment:
      - DISPLAY=host.docker.internal:0
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
    tty: true
